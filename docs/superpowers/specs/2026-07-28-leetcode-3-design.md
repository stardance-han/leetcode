# LeetCode 3 题目解法与项目规范设计文档

**日期：** 2026-07-28
**题目：** Longest Substring Without Repeating Characters（无重复字符的最长子串）
**设计目标：** 实现详细的教学性解法 + 建立 LeetCode 项目写作规范

---

## 一、3_lenghtOfLongestSubstring.py 文件设计

### 1.1 文件结构

```
文件头部
├── 题目描述（中英文）
├── 示例说明
└── 核心要点提示

解法实现
├── length_of_longest_substring()    # 最优解：滑动窗口+哈希表
├── length_of_longest_substring_set() # 经典解：滑动窗口+哈希集合
└── 辅助函数
    ├── print_test_results()         # 格式化输出测试结果
    └── run_all_tests()              # 执行所有测试用例

主程序
└── if __name__ == "__main__":      # 运行测试
```

### 1.2 解法详细设计

#### 解法1：滑动窗口 + 哈希表（最优解）

**函数签名：**
```python
def length_of_longest_substring(s: str) -> int
```

**核心思路：**
- 维护一个滑动窗口 [left, right]，表示当前无重复的子串
- 使用哈希表记录每个字符**最后出现的位置**
- 当遇到重复字符时，直接将 left 跳到重复字符之后（而不是逐个移动）
- 这是相比哈希集合解法的优化点

**算法步骤：**
1. 初始化：left = 0, max_len = 0, char_index = {}
2. 遍历字符串，right 从 0 到 n-1：
   - 如果 s[right] 在 char_index 中且索引 >= left：
     - 说明遇到了重复字符，将 left 更新为 char_index[s[right]] + 1
   - 更新 char_index[s[right]] = right
   - 计算当前窗口长度 right - left + 1
   - 更新 max_len = max(max_len, 当前窗口长度)
3. 返回 max_len

**复杂度分析：**
- 时间复杂度：O(n)，每个字符最多被访问两次
- 空间复杂度：O(min(m, n))，其中 m 是字符集大小，n 是字符串长度

**边界情况：**
- 空字符串：返回 0
- 单个字符：返回 1
- 全部重复：如 "aaaa" 返回 1
- 全部不重复：如 "abc" 返回 len(s)
- 重复字符在窗口外：如 "abba" 中，第二个 'a' 出现时，第一个 'a' 已不在窗口内

**优化点：**
- 相比哈希集合解法，此解法在遇到重复字符时可以"跳跃式"移动 left
- 不需要逐步缩小窗口，减少操作次数

#### 解法2：滑动窗口 + 哈希集合（经典解）

**函数签名：**
```python
def length_of_longest_substring_set(s: str) -> int
```

**核心思路：**
- 维护一个滑动窗口 [left, right]
- 使用哈希集合记录窗口内的字符
- 当遇到重复字符时，逐步缩小窗口直到无重复

**算法步骤：**
1. 初始化：left = 0, max_len = 0, char_set = set()
2. 遍历字符串，right 从 0 到 n-1：
   - 如果 s[right] 在 char_set 中：
     - 逐步移除 s[left] 并 left++，直到 s[right] 不在 char_set 中
   - 将 s[right] 加入 char_set
   - 更新 max_len = max(max_len, right - left + 1)
3. 返回 max_len

**复杂度分析：**
- 时间复杂度：O(2n) = O(n)，最坏情况下每个字符被加入和移除各一次
- 空间复杂度：O(min(m, n))

**边界情况：**
- 同解法1

**对比说明：**
- 此解法在遇到重复字符时需要逐步缩小窗口
- 逻辑更直观，适合初学者理解滑动窗口概念

### 1.3 测试用例设计

**测试用例列表：**

| 用例类别 | 输入 | 预期输出 | 说明 |
|---------|------|---------|------|
| 题目示例1 | "abcabcbb" | 3 | "abc" |
| 题目示例2 | "bbbbb" | 1 | "b" |
| 题目示例3 | "pwwkew" | 3 | "wke" |
| 边界-空串 | "" | 0 | 空字符串 |
| 边界-单字符 | "a" | 1 | 单个字符 |
| 边界-全重复 | "aaaa" | 1 | 全部相同 |
| 边界-全不重复 | "abcdef" | 6 | 全部不同 |
| 特殊-重复在外 | "abba" | 2 | 验证跳跃逻辑 |
| 特殊-中英文混合 | "a中文b中文" | 4 | Unicode字符 |
| 特殊-包含emoji | "😀a😀b" | 3 | Emoji字符 |

**辅助函数设计：**

```python
def print_test_results(func_name: str, results: list)
```
- 格式化打印测试结果，包括函数名、每个用例的输入、输出、是否通过

```python
def run_all_tests() -> None
```
- 运行所有测试用例
- 对比两种解法的结果
- 可选：测量并对比执行时间

### 1.4 注释风格规范

**注释结构模板：**

```python
def solution_name(params):
    """
    核心思路：
    [一句话概括算法本质]

    算法步骤：
    1. [第一步]
    2. [第二步]
    ...

    代码详解：
    关键代码处使用 # 思考： 或 # 关键： 注释

    复杂度分析：
    - 时间复杂度：O(...)
    - 空间复杂度：O(...)

    边界情况：
    - [情况1]
    - [情况2]
    ...

    优化点：
    - [为什么这样做更好]
    """
```

---

## 二、CLAUDE.md 文件设计

### 2.1 文档结构

```markdown
# LeetCode 练习项目指南

## 项目概述
[项目目的、目标受众]

## 文件命名规范
[命名规则示例]

## 代码结构规范
[标准文件结构模板]

## 注释写作规范
[详细的注释风格指南]

## 测试用例规范
[测试用例组织方式]

## 按难度分类指南
- 简单题规范
- 中等题规范
- 困难题规范

## 按类型分类指南
- 数组/字符串
- 链表
- 二叉树
- 动态规划
- ...
```

### 2.2 各章节详细内容

#### 项目概述
- 项目目的：系统化练习 LeetCode，培养算法思维
- 目标受众：准备技术面试的开发者
- 特色：详细的教学性注释，适合回顾和讲解

#### 文件命名规范
- 格式：`{题号}_{英文题目名}.py`
- 示例：
  - `1_two_sum.py`
  - `2_add_two_numbers.py`
  - `3_length_of_longest_substring.py`
- 注意：使用下划线分隔，题目名用小写

#### 代码结构规范

**标准模板：**

```python
"""
LeetCode {题号}. {题目英文名}

{题目中文描述}

{题目英文描述}

示例：
    {示例1}
    {示例2}
"""

from typing import ...  # 必要的类型导入

# 如果需要自定义类
class ClassName:
    def __init__(...):
        ...

# 核心解法
def solution_name(params) -> return_type:
    """
    核心思路：
    ...

    算法步骤：
    ...

    代码详解：
    ...

    复杂度分析：
    ...

    边界情况：
    ...

    优化点：
    ...
    """
    # 实现

# 辅助函数（如果需要）
def helper_function(...):
    ...

# 测试用例
if __name__ == "__main__":
    # 测试代码
```

#### 注释写作规范

**注释分层：**

1. **函数级注释（docstring）**
   - 核心思路：算法本质
   - 算法步骤：执行流程
   - 代码详解：关键代码段
   - 复杂度分析：时间和空间
   - 边界情况：特殊输入
   - 优化点：相比其他解法的优势

2. **行内注释**
   - `# 思考：` - 决策点的推理过程
   - `# 关键：` - 关键操作说明
   - `# 注意：` - 易错点提醒

3. **调试注释（可选）**
   - 在开发阶段使用 `print()` 调试
   - 提交前可注释掉

#### 测试用例规范

**测试用例选择原则：**
1. **题目示例** - 必须包含
2. **边界情况** - 空值、单元素、全相同、全不同
3. **特殊情况** - 算法特定的边界
4. **复杂情况** - 包含特殊字符、大数据量

**测试代码组织：**

```python
if __name__ == "__main__":
    # 题目示例
    print(solution_name(...))

    # 边界情况
    print(solution_name(...))

    # 特殊情况
    print(solution_name(...))
```

#### 按难度分类指南

**简单题（Easy）：**
- 重点：清晰的问题理解和直观的解法
- 注释：详细解释思路，适合初学者
- 测试：覆盖主要情况即可

**中等题（Medium）：**
- 重点：算法选择和优化
- 注释：包含复杂度分析和优化思路
- 测试：全面的边界和特殊情况

**困难题（Hard）：**
- 重点：问题分解和高级算法
- 注释：包含问题分析、算法选择、实现细节
- 测试：极端情况和性能测试

#### 按类型分类指南

**数组/字符串：**
- 常见模式：双指针、滑动窗口、哈希表
- 边界：空数组、单元素、全相同

**链表：**
- 常见模式：快慢指针、哑节点、递归
- 边界：空链表、单节点、环

**二叉树：**
- 常见模式：递归、层序遍历、分治
- 边界：空树、单节点、完全二叉树

**动态规划：**
- 常见模式：状态定义、状态转移、初始化
- 边界：最小规模、特殊情况

**图：**
- 常见模式：DFS、BFS、并查集
- 边界：孤立点、连通分量

---

## 三、实现要点

### 3.1 文件实现优先级
1. 先实现 3_lenghtOfLongestSubstring.py
2. 再实现 CLAUDE.md

### 3.2 质量标准
- 代码可运行，所有测试用例通过
- 注释完整，符合规范
- 中文表达清晰，无错别字
- 代码风格统一（参考现有文件）

### 3.3 验证方式
- 运行测试用例，验证输出正确
- 代码可读性检查
- 注释完整性检查

---

## 四、后续扩展

### 4.1 可能的改进
- 为每道题添加解题思路的 Markdown 文档
- 按难度/类型建立索引文件
- 添加运行时间统计

### 4.2 项目管理
- 使用 Git 版本控制
- 每道题一个 commit
- Commit message 格式：`LeetCode {题号}: {题目名}`
